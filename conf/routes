# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /audit/:userid              controllers.AuditController.getAuditByUserId(userid: Long)
POST    /audit                      controllers.AuditController.addAudit(request: Request)
